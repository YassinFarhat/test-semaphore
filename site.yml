---
- name: Create text file in root directory
  hosts: all
  become: yes
  remote_user: root
  tasks:
    - name: Create a.txt file
      copy:
        content: "File created by Ansible\n"
        dest: /root/a.txt
        owner: root
        group: root
        mode: '0644'
